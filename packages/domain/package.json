{
  "name": "@groundtruth/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Entities, value objects and business rules. Zero framework imports, zero dependencies — by design (see DECISIONS.md D-001).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {}
}
